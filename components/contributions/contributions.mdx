## 使い方

### セットアップ

以下を実行してインストールします:

```bash
npm install --save next react react-dom
```

次に、package.json にスクリプトを追加してください。

```json
{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start"
  }
}
```
